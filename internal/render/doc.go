// Package render serializes validated evaluation results as canonical JSON or
// a derived human-readable text view. InputSummary map keys use encoding/json's
// lexicographic ordering in JSON and the same explicit ordering in text.
package render
